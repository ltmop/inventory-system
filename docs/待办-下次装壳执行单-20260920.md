# 待办 · 下次装壳执行单

> 目标版本 **1.1.11**（当前 1.1.10）。
> 配套：流程与原理见 `docs/运维-发壳-Runbook.md`；边界靠 `node scripts/shell-release-audit.mjs` 算，不靠记性。
> 生成：2026-09-20（当日实测数据，逐条已注明来源）。

---

## 0. 为什么现在要发壳（今天实测的判据）

```bash
node scripts/shell-release-audit.mjs      # ③ 真·壳层 ≠ 空 → 必须装壳
node scripts/release.mjs --check           # 2026-09-20 实测：**直接报错**
```

`release.mjs --check` 的原话：

```
Error: 本地 1.1.10 不高于服务器 1.1.10（版本必须递增）
```

服务器更新源 `updates/latest.yml` 实测 `version: 1.1.10`，本地 `package.json` 也是 `1.1.10`
→ **第一步必须升版本号**，否则发布链会自己把自己挡住。

---

## 1. 本次要装进壳里的东西

### 1.1 必发（已提交，只等发壳生效）

| 文件 | 改了什么 | commit | 不发会怎样 |
|---|---|---|---|
| `electron/main.js` | ① `console.*` 包 `try/catch`（stdout 断了不再变成 `uncaughtException`）② 渲染进程崩溃自动重载**加节流**（60 秒最多 3 次） | `eb36284` | crash.log 继续被 EPIPE 假崩溃淹（真崩溃难查）；万一某版前端一加载就崩会**无限闪屏** |

> 为什么必须装壳：`electron/**` 里只有 `commands.js + commands/**` 及其相对 import 在热更闭包内
> （`scripts/lib/code-closure.mjs`），`main.js` 不在 → **热更推不到**。

### 1.2 建议一并做（等你一句话我就提交，只在这个壳版本里生效）

**`electron/preload.cjs`：把中心库配置的"补齐"改成"对齐"**

现状（`preload.cjs:12-15`）——只在 localStorage **没有**该键时才用 `central.json` 补齐：

```js
const cfg = ipcRenderer.sendSync('cloud:centralSync') || {}
if (cfg.url && cfg.token && !localStorage.getItem('fi-central-url')) {
  localStorage.setItem('fi-central-url', cfg.url)
  localStorage.setItem('fi-central-token', cfg.token)
}
```

建议改成（main 的 `central.json` 是单一事实源，不一致就以它为准；它非空才覆盖）：

```js
const cfg = ipcRenderer.sendSync('cloud:centralSync') || {}
if (cfg.url && cfg.token) {
  try {
    if (localStorage.getItem('fi-central-url') !== cfg.url ||
        localStorage.getItem('fi-central-token') !== cfg.token) {
      localStorage.setItem('fi-central-url', cfg.url)
      localStorage.setItem('fi-central-token', cfg.token)
    }
  } catch { /* 隐私模式写不进：本次会话按 localStorage 里已有的走 */ }
}
```

| | |
|---|---|
| **好处** | 下次再轮换中心库令牌（或改中心库地址），**各设备只要重启即可自动对齐**，不用人工粘贴、也不用我去改 LevelDB（2026-09-20 那次轮换就是被这条守卫挡住的，见 Runbook §5.3②） |
| **风险** | 改变了启动时"谁说了算"的优先级：从 localStorage 优先变成 `central.json` 优先 |
| **要测的 3 个场景** | ① 两边一致 → 不写（无副作用）② `central.json` 新、localStorage 旧 → 覆盖成新 ③ `central.json` 空（未配中心库）→ **什么都不做**，不能把用户本地配好的配置清掉 |
| **配套断言** | 加"cfg 非空才覆盖"与"两边一致不写"两条静态断言，防止以后被改回单向守卫 |

### 1.3 ✅【已修 · 2026-09-21 · commit a2636a4】热更就能修的那个真 bug（analyticsTop 的 p.name）

`electron/commands/analytics.js` 的 `analyticsTop` 引用了 **products 表不存在的列 `p.name`**：

| 行 | 现状 | 后果 |
|---|---|---|
| 64 | `SELECT … p.name, p.brand, … FROM transactions t JOIN products p …` | **抛错** `no such column: p.name` → `/api/analytics/top` 实测返回 **400** |
| 68 | `try { … SELECT id, name, brand … } catch { lookup = [] }` | 作者其实**猜到 name 可能不存在**，这里包了 try/catch；但结果永远是空 → 第 86 行 `name: m?.name ?? '未知商品'` → **所有商品显示「未知商品」** |

- 正确性依据：`products` 表（`electron/db.js:44-66`）**没有 name 列**，只有 `brand/model/sku_code`；
  全仓扫描确认这两行是**唯一**的商品 name 引用（其它 `s.name` 是 `suppliers.name`，有效列）。
- ✅ **已修（commit `a2636a4`，已推 `origin/main`）**，实际改法与下面这份计划略有出入，以代码为准：
  ① 第 64 行删掉 `p.name`/`p.brand`/`p.model`/`p.sku_code` 这 4 个死列，**连那个白 JOIN 一起删**
  （`t.product_id` 本来就在 transactions 上）；② 第 68 行 lookup 去掉 `name`，**try/catch 保留**（计划建议删，保留更不动现状）；
  ③ 第 86 行不自己拼名字，**复用既有的 `helpers.productLabel`**（品牌+型号 → 回退 SKU），
  与 `clearance.js` / `pricing.js` / 手机端同一口径，不再造第三份拼名字的代码。
- **影响面（2026-09-20 实测）**：中心库 `/api/analytics/top?n=5` → **HTTP 400** `no such column: p.name`；
  同一批里 `/api/analytics/trend` 正常（200），所以**只有 top 这一条坏**。
  受影响的调用方：`scripts/inv-cli.mjs:123`（CLI 的 top）、`analytics:top` invoke 通道，
  以及**三份对外文档把它写成交付接口**（`docs/命令接口-接口文档.md` / `docs/进销存数据分析接口.md` /
  `docs/进销存系统Agent接入指南.md`）—— 即"文档承诺了、调用会 400"。
  桌面端界面与手机端**不受影响**（全仓 grep 确认没有任何渲染层代码调 `analytics:top`）。
- ✅ **覆盖缺口已补**：原来 835 条断言里没有一条打这个函数。现在补了 **8 条真跑断言**
  （用真实 schema 建库跑 `analyticsTop`：退货冲减 / 换货退旧不计 / 营业额降序 / 件数毛利 / 名字回退 SKU / n 的上下界）
  → **835 → 843 全过**，`npm run build` 通过。
- ✅ **线上已修（同日 2026-09-21）**：那个 400 是**中心库服务器**那份代码报的（该端点由中心库进程提供），
  所以正解是部署中心库 —— 已先 diff 原件（只差这 4 处）→ `cp -p` 备份 `analytics.js.bak-20260921-221100`
  → 上传 → `pm2 restart inventory-app`；公网 `GET /api/analytics/top?n=3` 实测 **200** 且名字正确，
  同模块 `trend`/`stockValue`/`overview`/`category` 全部 200。
- ✅ **已修（2026-09-21，仓库侧）**：`electron/preload.cjs` 白名单补上了 `ai:setEndpoint` / `ai:syncCentral`
  （`main.js:427-428` 本来就有实现、src 的 AI 设置页在调用）。**已装的 1.1.13 仍调不动它们** ——
  这条从**真机 asar** 里取了硬证据：壳内 `electron/preload.cjs`（9443 字节，20 条 `ai:`）这两条 = **false**、
  同一 asar 的 `main.js` = **true**。⇒ 「AI 设置保存没反应」由"推断"升级为"取证"；**生效必须随下次装壳**。
- ✅ **同时补了闸门**：`test-backend.mjs` 新增 3 条断言（通道闸门 + 防假绿 + **反例自检**），
  判据与 `build-web-bundle.mjs` 第 ④ 步、客户端运行期**完全同一套** → 把"发版时才红"提前到"提交时就红"；
  断言 **843 → 846** 全过，`--check` 已转绿。
  ⇒ **装完这次壳之后，前端热更这条路才重新可用**（在装壳前发出去也没机器会收）。

---

## 2. 版本号两处同改（最容易漏的一条）

> ⚠️ **本节的目标版本号（1.1.11）已过期**：仓库与线上都已是 **1.1.13**（`package.json` 与更新源均已到 1.1.13，
> `public/web-version.txt` = `1.1.13.0`）。**下一版按 1.1.14 起算**；下表只作"两处要同改"的示范，别照抄数字。

| 文件 | 从 | 到 |
|---|---|---|
| `package.json` 的 `version` | `1.1.10` | `1.1.11` |
| `public/web-version.txt` | `1.1.10.3` | `1.1.11.0` |

**为什么第二处也要改**：`electron/webUpdate.js:338` 的判据是
「热更包 `webVersion` ≤ 内置版本 → 弃用该热更包」。
内置版本提到 `1.1.11.0`（> `1.1.10.3`）后，老热更包自动作废，
用户装上新壳看到的是**新壳自带的前端**，不会出现"新壳 + 旧热更包"的半新半旧状态。

---

## 3. 执行

```bash
# ① 改上面两处版本号
# ② 前端自检（官网版本号改写逻辑，固定夹具 + 线上实页，不会碰服务器）
npm run check:web

# ③ 只做前置检查，不碰服务器（注意：升完版本号才能过）
node scripts/release.mjs --check

# ④ 全流程：[1/5]检查 → [2/5]打包 → [3/5]产物 → [4/5]部署更新源
#            → [4b]官网下载页 → [5/5]三样验证 → [5b]官网版本验证
node scripts/release.mjs
```

单独打壳可用 `npm run dist`（`scripts/dist.cjs`：先 `npm run build` 再 electron-builder 出 NSIS，
产物先落临时目录再拷回 `release/`，避开工作区文件监控导致的 EPERM）。
**正常发版请走 `release.mjs`** —— 它会做版本递增检查、部署前备份 `latest.yml`、三样验证，
并保证**只部署产物、不碰服务器源码**。

---

## 4. 发完必做（`release.mjs` 不管的几件）

| # | 事 | 现状（2026-09-20 实测） |
|---|---|---|
| 1 | **中心库 `/app` 浏览器版 `dist` 同步** | `https://app.junchengzn.com/app/web-version.txt` = **1.1.10.0**，而桌面端已是 1.1.10.3。手机/浏览器打开 `/app` 看到的是这一份。做法：先备份、只传产物（与 `electron/` 同样口径） |
| 2 | **本机（收银机）装新壳** | 装了才拿到 §1.1 那些壳层修复（热更给不了） |
| 3 | **门店其他机器** | 应用内更新或官网重装 |
| 4 | **清 `updates/web/` 旧版本目录** | 已累积 1.1.9.1 / 1.1.10.1 / 1.1.10.2 / 1.1.10.3，只留最近 1–2 个即可（旧版是回滚资产，不是必须全留） |
| 5 | **`package-lock.json` 漂移** | 停在 `name=fishing-inventory / version=1.0.14`（与 `package.json` 早已矛盾）。发布流程不读它，但该顺手修：`npm install --package-lock-only` 或删掉 |
| 6 | **中心库部署的 `electron/` 与 `dist/` 与仓库对齐** | 今天实测 `server.js` 哈希**已一致**（`2906b213…`），`commands/` 也在 → 这条目前**只差 `dist/`**；但建议每次发壳后复核一次（`sha256sum` 对比），别靠"上次传过" |

---

## 5. 验收

- [ ] `npm run build` 通过；`node scripts/check-assertions.mjs` 通过（当前基线 **835**，只增不减）
- [ ] `release.mjs` 的三样验证全绿（GET 下载 / sha512 / 版本号）
- [ ] `npm run check:web` 三处版本号（下载页 / 产品页 / 首页数据区）都是 1.1.11
- [ ] **本机装壳后**：
  - [ ] `crash.log` 里不再出现 `uncaughtException … EPIPE`（假崩溃应绝迹）
  - [ ] `web-current.json` 的 `lastRollback.reason` 出现 **「内置版本已更新，弃用热更包」**
        （证明 `1.1.10.3` 被正确弃用、走的是新壳自带前端）
  - [ ] 顶栏/设置里版本显示 1.1.11
- [ ] 中心库 `/app/web-version.txt` = 1.1.11.0

---

## 6. 回滚

| 退什么 | 怎么做 |
|---|---|
| 退**更新源指针** | `sudo cp /opt/inventory-cloud/updates/latest.yml.bak-<ts> /opt/inventory-cloud/updates/latest.yml` |
| 退**热更前端** | 把 `updates/web/latest.json` 换回上一个 `.bak-*`，或删掉 `latest.json`（客户端不动，退到壳内置版本） |
| 退**壳** | 官网/更新源换回上一版 exe，让用户重装（Windows 装低版本通常要先卸载） |

---

## 7. 可勾选清单（执行时照着划）

```
[x] 1  改 package.json version -> **1.1.14**（2026-09-21, 750d6ab）
[x] 2  改 public/web-version.txt -> **1.1.14.0**（同上）
[ ] 3  （可选）提交 preload.cjs 的"对齐"补丁 + 两条断言
[x] 4  ✅ 已修 analytics.js 的 p.name（2026-09-21, a2636a4）+ 8 条真跑断言（843 全过）；**线上已部署中心库，实测 200**
[x] 5  npm run check:web：**A 段与 C 段全过**；B 段 6 条红在**本地站点副本未跟上**（与发布无关，见发布记录十三补）
[x] 6  node scripts/release.mjs --check 通过（✅ 可发布 1.1.14）
[x] 7  node scripts/release.mjs **全流程绿**（2026-09-22 发布完成：updates + download + 官网六页；三样验证 + 官网验证全过）
[ ] 8  本机装新壳 + 复核 crash.log / web-current.json
[ ] 9  中心库 /app dist 同步 + 复核 web-version.txt
[ ] 10 门店其他机器升级
[x] 11 清 updates/web/ 旧目录 —— 2026-09-22 删 1.1.9.0 / 1.1.9.1 / 1.1.10.1（**79M → 32M**），保留当前发布版 **1.1.10.3** + 上一版 1.1.10.2；删前打包备份并取回本地 `D:\进销存备份\updates-web-old-3.tgz`；**复核 latest.json 仍指向 1.1.10.3**（不会造成热更 404）
[x] 12 修 package-lock.json —— 2026-09-22 `npm install --package-lock-only` 重生成：name/version 由 `fishing-inventory/1.0.14` → **`inventory-system/1.1.14`**（另加 68 行 tailwind 可选嵌套依赖，**真实依赖版本无变动**）；854 项断言全过
[x] 13 ✅ preload.cjs 补 ai:setEndpoint / ai:syncCentral（2026-09-21, 仓库已改）+ 3 条通道闸门断言（846 全过）—— **生效随本次装壳；装完前端热更才重新可用**
[ ] 13 更新 docs/发布记录.md（发壳记录 + 回滚路径）
```

---

## 8. 附：已过期的旧待办（别照旧单子做）

发布记录里那份 2026-09-19/20 的待办单有几条**今天实测已经变了**：

| 旧条目 | 今天实测 |
|---|---|
| 「本机那份库仍是旧的 324 商品 / 7609 件，没清」 | **已过期**：本机库实测 商品 0 / 批次 0 / 流水 0（分类 20 / 单位 24 保留） |
| 「中心库的 `/app` 网页版仍是 1.1.9 的 dist」 | 已升到 **1.1.10.0**（仍落后桌面端的 1.1.10.3） |
| 「本机现在是 1.1.9」 | 已过期：本机是 1.1.10，且前端热更到 1.1.10.3 |

> ⚠️ 另外记一条**之前写错、今天纠正**的事实：台账里曾说"本机库与中心库账本都已清空（商品 0）"，
> 今天用中心库 API 实测 **不成立** —— 中心库有 **190 个 SKU / 3728 件 / 库存价值 ¥8516.50 / 低库存 135 个**
> （`/api/low-stock` 能列出「台钓线组（散装）」这类真实商品）。**被清空的是本机那份副本**，
> 中心库（真正在用的账本）没有清。详见发布记录同日条目。
